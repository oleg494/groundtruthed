# SDK-путь (только scripts/explore.py) мигрирован на t-tech (домен *.tbank.ru).
# Пакет НЕ на pypi.org — ставится из приватного индекса T-Bank:
#   pip install t-tech-investments --index-url \
#     https://opensource.tbank.ru/api/v4/projects/238/packages/pypi/simple
t-tech-investments>=1.49.0
# Старый SDK больше не нужен (был не установлен; explore.py откатится на него,
# если вдруг окажется в окружении):
# tinkoff-invest>=0.3.0
python-dotenv>=1.0.0
pandas>=2.0.0
requests>=2.31.0
pytest>=7.0.0
